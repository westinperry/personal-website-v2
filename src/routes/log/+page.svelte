<script lang="ts">import {dateLabel} from '$lib/format';import PhotoImage from '$lib/components/PhotoImage.svelte';import ScrollCue from '$lib/components/ScrollCue.svelte';let{data}=$props();</script><svelte:head><title>Log — Westin Perry</title><meta name="description" content="A factual timeline of recent life and activity."/></svelte:head>
<section class="page-intro opening-screen"><p class="eyebrow">Log</p><h1>A record of<br/>what happened.</h1><p>A factual timeline. Short entries, kept in order.</p><ScrollCue target="log-content"/></section><div class="ledger" id="log-content">{#each data.entries as entry}<article><time datetime={new Date(entry.event_date).toISOString()}>{dateLabel(entry.event_date,{month:'short',day:'numeric',year:'numeric'})}</time><div><h3>{#if entry.related_url}<a href={entry.related_url} rel="noreferrer">{entry.title}</a>{:else}{entry.title}{/if}</h3>{#if entry.description}<p>{entry.description}</p>{/if}{#if entry.photo_key}<div style="max-width:420px;margin-top:1rem"><PhotoImage photo={{storage_key:entry.photo_key,alt_text:''}}/></div>{/if}</div></article>{/each}</div>
